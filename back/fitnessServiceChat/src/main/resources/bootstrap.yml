spring:
  server:
    port: 8120

  application:
    name: chat-service

  profiles:
    active: default

  cloud:
    config:
      uri: http://configClient:configClient@localhost:8060
eureka:
  client:
    enabled: false

messages:
  maxResult: 10