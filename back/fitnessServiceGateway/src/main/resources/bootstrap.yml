spring:
  application:
    name: gateway-service
  main:
    web-application-type: reactive

  profiles:
    active: default

  cloud:
    config:
      uri: http://configClient:configClient@config-service:8060